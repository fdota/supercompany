[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Regola per le funzioni API (FONDAMENTALE)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Regola "catch-all" per l'applicazione
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions."save-to-sheet"]
  timeout = 25